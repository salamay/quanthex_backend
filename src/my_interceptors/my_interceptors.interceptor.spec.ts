import { MyInterceptorsInterceptor } from './my_interceptors.interceptor';

describe('MyInterceptorsInterceptor', () => {
  it('should be defined', () => {
    expect(new MyInterceptorsInterceptor()).toBeDefined();
  });
});
